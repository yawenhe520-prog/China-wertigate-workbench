{
  "name": "china-wertigate-project-workbench",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Document-grounded local project workbench",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node --test tests/*.test.cjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
